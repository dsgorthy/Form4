fastapi
uvicorn[standard]
sqids
slowapi
psycopg2-binary
# These were installed by a hand-maintained `pip install ...` line in
# ci.yml rather than declared here, which is the same gap beautifulsoup4 fell
# through. Declared where they belong so CI installs what production does.
# Pinned 2026-09-10. Unpinned, this jumped to 15.x, which stopped making
# StripeObject a dict subclass; every .get() in the webhook handler raised and
# the endpoint returned 500 to every event until it was found. Bump
# deliberately, with api/routers/webhooks.py re-read.
stripe==15.6.1
PyJWT[crypto]
resend
pydantic
