# Handoff — 2026-09-10, end of session

Written for the next session, which may be a different model at a different
effort level. **Everything below is either verified state or an explicit
instruction. Where something is unverified, it says so.**

Plan this feeds: `docs/growth_plan_2026-09.md`. Artifact:
https://claude.ai/code/artifact/44461053-fc8c-4142-b1f0-6bc8ac605d9b

---

## READ THIS FIRST — the ~05:00 PT run on 2026-09-11

The session usage limit reset at 04:00 PT. A scouting pass at 23:30 (10
read-only agents, 2 skeptics each, 1 critic) got **six scouts through before
the limit hit**; no skeptic and no critic ran, so the estimates below are
scout numbers, unattacked. **Silver, price_quality, backup design and the
remaining launchd migrations were NOT scouted** — re-scout before touching.

Rules for this run, no exceptions: no Dagster restart, no plist load/unload
on Studio, no `studio deploy`, no DB writes on Studio, no Clerk or Stripe
writes. **Work on a branch `overnight/2026-09-11` and push the branch only.**
Do not push to main — **confirmed 2026-09-11 09:00: `.github/workflows/ci.yml`
has a `deploy-prod` job gated on `refs/heads/main` that runs
`git pull && ./deploy/deploy.sh --env prod` on a self-hosted runner on
Studio. Every push to main is a production deploy, docs-only included.**
(Which also means a manual `studio deploy form4` after a push is redundant.)

Execute in this order. After each, commit, and append a one-line result to
this section so the morning reader sees what happened.

1. **Watchdog checks status, not just recency** (~2h, low, Mini-only). The
   scout wrote the design: replace `SERVICE_HEARTBEAT_SQL` with a 4-column
   query — service, `max(started_at)` over ALL rows (a hung `running` row
   still ages out), latest non-`running` status via
   `(array_agg(status ORDER BY started_at DESC) FILTER (WHERE status <>
   'running'))[1]`, and the first line of `error_message` LAST so a pipe
   inside it cannot shift columns. Extract the consumer into a pure
   `evaluate_service_heartbeats(rows, now) -> (problems, lines)` so it is
   testable with psql-shaped strings, exactly as `_parse_ts` is. Two
   independent verdicts per service — stale and failing — because a service
   can be both. Tests in `tests/unit/test_watchdog_status.py` using the
   importlib loader from `test_watchdog_parse.py:22-28`; pin the literal
   2026-09-10 22:50 row `notification_scanner|...|failed|TypeError: '<' not
   supported...` and assert one status problem and no staleness problem.
   The SQL must contain no `"` or `$` — `ssh_psql` wraps it in double quotes.
   Run tests on both interpreters: the venv, and
   `/usr/bin/python3 -m pytest --noconftest tests/unit/test_watchdog_*.py`
   (conftest imports numpy, which system python lacks). Verify with
   `--dry-run`.
2. **Mini watchdog → calendar** (~45m, low, Mini-only).
   `python3 scripts/launchd_interval_to_calendar.py
   ~/Library/LaunchAgents/com.derekg.offbox-watchdog.plist`, same for the
   repo copy `scripts/launchd/com.derekg.offbox-watchdog.plist`, then
   `launchctl bootout gui/$(id -u)/com.derekg.offbox-watchdog` and
   `bootstrap`. The converter strips the header comment — re-add it. Verify:
   `launchctl print gui/$(id -u)/com.derekg.offbox-watchdog` shows
   `com.apple.launchd.calendarinterval` and no `run interval = 1800`.
3. **Rehearse `verify_bronze.py`** (~2h, read-only on Studio). It has run
   once, on 2026-09-06, never at scale. The scout found a real defect:
   `check_coverage` (`verify_bronze.py:139-159`) never counts accessions in
   `bronze.edgar_index` without a submission row — **it passes at 55% today
   and would pass at 100% with silent holes.** Fix that (the script is
   unscheduled; editing it is safe), then run quick mode on Studio, record
   wall time to `/tmp/verify_bronze_rehearsal_quick.log`. Do not take the
   fetcher's advisory lock.
4. **StockTwits `--write`** (~2h, low). Make it write
   `stocktwits_{date}.txt` WITH the schedule header the print path carries
   (they drifted at `63d046b`), and add a way to land it in `data/content/`
   on the Mini where Derek reads it — rsync from Studio, or run via ssh and
   write locally. `test_stocktwits_rate_limits.py` and
   `test_stocktwits_posts_are_staggered.py` must stay green.
5. **`emailed_at` bug — code and tests only.** Scout claim:
   `_maybe_send_realtime_email` stamps `emailed_at` without checking that
   `send_email()` returned True, so a Resend failure is recorded as sent.
   Fix the return path, add tests (daily-frequency DIRECT not stamped;
   Resend failure not stamped). **Do NOT run the data-repair UPDATE and do
   NOT deploy** — both wait for Derek. Caveat: this scout tripped the
   security classifier; re-derive its claims from the code before trusting
   any of them.
6. **If time remains: re-scout Silver only.** Read
   `docs/silver_layer_model.md`, the bronze schema, and find the existing
   Form 4 XML parser in `strategies/insider_catalog/`. Design on paper. Do
   not create a schema or table on Studio.

**Deferred, do not start:** tier metadata on signup (needs a Clerk Dashboard
webhook + signing secret only Derek can create; the API already treats a
missing tier as free, so nothing is broken); everything under "Do NOT start
without Derek" below.

Results appended by the run:
- 09:05 — item 2 DONE inline: `com.derekg.offbox-watchdog` converted to
  `StartCalendarInterval` (:00/:30), header comment preserved, reloaded;
  `launchctl print` shows two `calendarinterval` streams, no `run interval`.
  Pre-change plist saved to `~/retired_plists/…pre-calendar-20260911`.
- 09:05 — items 1, 3, 4, 5 dispatched to isolated worktrees on branches
  `overnight/2026-09-11-<key>`, two reviewers each; Silver design (item 6)
  dispatched read-only. Nothing pushed.
- 15:05 — items 1, 4, 5 MERGED to local main (not pushed). Item 1 and 5
  approved by two reviewers each; item 4's reviewers hit the limit, diff read
  by hand, one fix added (pin `/opt/homebrew/bin/python3` in the ssh
  delegation). Suite 1,962 expected green. Item 3 (verify_bronze) and item 6
  (Silver design) did not start — limit. Mini watchdog calendar ticks
  confirmed firing :00/:30 from 13:30 onward.
- CORRECTION: the scanner outage was 3,811 failed of 3,912 runs, not
  "4,182 with zero successes" — 101 succeeded, all on 09-01 (which is why the
  watermark sat at 09-01). Implementer re-measured; earlier figure was mine.
- emailed_at: my premise was half wrong. The 7,059 unstamped rows are
  emailed=2 EXPIRED — by design, never stamped. The real defect is 18 rows
  phantom-stamped SENT at creation (daily-frequency users' DIRECT alerts).
  Code fix merged; the 18-row repair UPDATE is Derek's call (see item 5 notes
  in git log for the exact statement). Until deployed, daily users'
  portfolio_alerts keep being swallowed at creation.
- 16:31 — PUSHED `7d61934`; CI deploy-prod confirmed Studio at that commit,
  smoke 200/200, scanner green on the new code. **16:40 — 18-row repair RUN**
  (Derek approved): transactional, row-count guard, `lock_timeout=5s`;
  matched 18, updated 18, 0 phantom rows remain. The 8 real portfolio alerts
  from 09-10 go out in the 09-12 07:00 digest.
- 16:33 — item 3 DONE: `verify_bronze.py` coverage check now names what is
  missing (8 tests, all fail on the old script; committed `b3ff28e`, NOT yet
  pushed). Rehearsed on Studio from /tmp, `--quick --expect-partial`:
  corpus 4,137,834 / stored 2,549,052 (61.6%) / **missing 1,588,773** (named,
  oldest-first, 2013 accessions) / orphaned 0. **Integrity over 2.55M rows:
  79.3s → projects to ~130s at 100%, inside `statement_timeout=900s`.**
  Whole quick run 81s. It FAILED on one real thing: **9 accessions with
  HTTP 404 from SEC**, all 2013, all fetched today — `--retry-failed` will
  keep retrying them hourly forever. Likely permanent (old-filing URL form?);
  someone should look at one by hand before 09-15 rather than let the
  verifier fail on them at 100%.
- Bronze content format, for Silver: `content` is the full
  `<SEC-DOCUMENT>` .txt submission wrapper (SEC-HEADER + embedded `<XML>`
  ownershipDocument), NOT bare XML. A Silver builder must unwrap it first.
- 17:05 — item 6 DONE as design: `docs/silver_build_plan.md` (commit
  `37c7e18`). DDL for `silver.form4_transaction` + `silver.form4_parse`,
  self-healing builder reusing `parse_form4_xml_full` and the existing SGML
  unwrap, `--assess` reusing the 09-06 `price_quality` criterion, and a
  parity check whose load-bearing row is `silver.price_per_share =
  trades.price_as_filed`. ~5h of local build is safe unattended; applying the
  DDL on Studio is Derek's. **Docket for the day is closed** — every item
  that did not need Derek is done; three local commits await a push.

## 2026-09-15 23:00 — form4.app + trytailorly.com 502, ~12 min

Lima's ssh control connection to the Colima VM dropped at 22:53:08 and every
port forward (80, 8080, docker.sock) died with it while the VM, `dockerd` and
all ten containers kept running. Cause: `mountInotify: true` — Colima's
inotify daemon ssh'd into the VM to `chmod` every host log write (uptime.log
once a minute) through that same connection. Stopgap `ssh -L` restored both
sites at 23:05; proper fix 23:25–23:31: `mountInotify: false` + restart via
`launchctl kickstart` (`brew services restart` stopped it and never started
it). Full mechanism and the fast tell in memory
`project_2026-09-15_colima_forwards_outage`. Follow-up: `uptime_monitor.sh`
records `pipeline_runs.status=ok` while the site is down (it exits 0), so
the watchdog's status check cannot see a site outage through it. A Colima
restart also drops the tailorly and design-quiz tunnels despite
`KeepAlive=true` — kickstart both after any restart (in memory).

## 2026-09-13/14 — weekend and Monday

- 09-13 — Silver BUILT (`pipelines/silver/`: faithful parser, builder,
  assessor, parity; 9 tests; `edea956`). The plan's "reuse the live parser"
  was wrong: `parse_form4_xml` drops lines five ways and coerces to float.
  Silver has its own. **DDL `migrations/2026-09-13_silver.sql` still not
  applied** — the sandbox classifier blocks me; Derek runs
  `psql -d form4 -f migrations/2026-09-13_silver.sql` on Studio, then
  `python3 pipelines/silver/build.py --limit 10000` → `assess.py` →
  `parity.py`.
- 09-14 08:18 — **Bronze COMPLETE**: 4,137,834/4,137,834. 09:13 —
  **`verify_bronze.py` full run PASSED**: 0 failed / 0 orphaned / 0 missing,
  integrity 82.7s, daily-index x-check 0 missing, 200 re-fetched from SEC
  identical. 225s total.
- 09-14 — **Every Friday insider buy was never alerted.** Decision-audit
  rows by filing weekday, 30d: Mon 3,058 / Tue 3,553 / Wed 1,720 / Thu 439 /
  **Fri 0**. `filing_lookback_days: 2` was calendar days, so Monday's window
  began Saturday. Fixed to trading days (`_lookback_start`, `009cc27`,
  8 tests). Friday 09-11's filings are inside today's window and will be
  scanned by the next tick after Studio pulls — a trading day late, not
  never. The Monday paper monitor had been failing every Monday saying
  exactly this; its own diagnosis ("preflight likely halted") was wrong.
- 09-14 — heartbeat file frozen since the Dagster one-shots (12 days,
  `--once` never wrote it) — fixed. freshness_probe judged raw hours where
  the preflight judges business hours — 194 false weekend criticals/week —
  fixed to use the same function.
- 09-14 09:2x — Friday 09-11 filings CONFIRMED scanned on the first tick
  after the pull (15 audit rows, 6 for 09-11, 4 passed on A-List).
- 09-14 — **Silver DDL applied, smoke build 760 acc/s with 0 parse errors,
  full build running** (`/tmp/silver_build_full.log`, nohup on Studio;
  ~4.14M at ~650/s ≈ 1.7h from 09:30). Parser is v2 (CIKs kept ten-digit
  as filed; v1 stripped them and joined nothing — tables truncated and
  rebuilt). 22 parse errors so far, all one filer's malformed 2007 XML
  (0000020171-…), recorded as receipts.
- **Parity (at 192k accessions):** match 45.8%, `price_overwritten` 8 —
  the row that proves the model: IHT's owner 0001055365 has filed prices
  like 17,372.52 that trades overwrote to 1.4035; Silver holds the filed
  number. `silver_only` (non-derivative) 14.3% = lines trades never had;
  `silver_only_derivative` 22.9% (trades never held derivatives for most
  of its history); `trades_only` 3.5%. Remaining "shares" mismatches are
  positional drift where trades is missing sibling lines — read them as
  losses, not disagreements.
- **`trades` defect, not Silver's — CORRECTED 09-14 16:00 after a read-only
  scout.** 13,780 rows filed 2026-01-02→03-16 (source `edgar_live`) with NULL
  `rptowner_cik` and `security_title`. `insider_id` IS populated (the column
  is NOT NULL; my earlier "NULL insider_id / on no insider page" was wrong).
  They were written **2026-03-12..16 by a pre-git `insert_trades`** that did
  not write the extended block — not by the 08-26 reload. The same defect sits
  on **34,272 rows filed 2016–2019** from the same March burst. Then the 08-26
  reload loaded the same lines from the SEC datasets (`sec_form345`) without
  recognising them: **6,418 lots now exist twice under the same insider —
  $22.3B double-counted in company aggregates today.**
  Repair: `scripts/repair_orphan_trades_from_silver.py` — fills from Silver
  (unique match 12,669; 689 joint filings resolved by name; 242 identical lots
  get cik/title but no line_no; 180 unmatched are fractional shares truncated
  into `qty bigint`), then marks the same-insider `sec_form345` copies
  duplicate. Backup table `repair_orphans_20260914_before`, row-count guards,
  rollback on deviation. **Dry run 2026: will_fill 13,600 / unmatched 180 —
  matches the scout exactly. Apply is blocked for Claude by the classifier
  (UPDATE on trades); Derek runs it.** 2016-2019 dry run: will_fill 34,207
  of 34,272, unmatched 65, dedupe will retire 8,950 reload copies ($16.7B).
- 09-14 16:2x — **Silver COMPLETE: 4,136,977 accessions parsed (856 parse
  errors = malformed 2007 XML from one filer family, 1 no-document), 11.56M
  lines.** Assess over 10.66M priced lines (after the `no_price` correction —
  1.77M zero-price grant/exercise lines had been labelled outside_band):
  **ok 2.39M / outside_band 268,840 (2.5%) / implausible 5,534 / no_price
  3.46M / no_reference 4.54M** (5.95M of the no_reference are pre-2016, before
  the price history starts; the 2016+ remainder is non-tickers and
  delistings). **Full-corpus parity** (server-side,
  147s): match 36.8% (4.44M) / silver_only_derivative 24.8% / mismatch 18.1% /
  silver_only 16.4% (1.98M non-derivative lines trades never had) /
  trades_only 4.0% (480k — mostly the 48k owner-less rows, which cannot join
  until repaired) / **price_overwritten 130**. Reports: `parity.py --summary`.
  assess rewritten (temp band table; was a 12-min batch) and parity gained
  --summary; both committed locally (`980081d`), not yet pushed.

---

## State at handoff — all green, verified 23:19 PT

| | |
|---|---|
| git | clean, `4456ddd`, pushed to `origin/main` |
| Studio HEAD | `4456ddd`, matches |
| tests | 1,936 passed, 27 skipped |
| Mini disk | **153 GB free, 64% used** (was 7 GB / 99% this morning) |
| Studio disk | 277 GB free |
| Dagster | no failing jobs; `job_ops_notifications_5min` green since 23:15 |
| `check_scheduled_work.py` | `OK: reality matches the registry` |
| launchd | **zero `StartInterval` agents loaded** — keep it that way |

---

## RUNNING — DO NOT KILL

**Bronze backfill.** `scripts/fetch_bronze.py`, PID **53889** on Studio, started
2026-09-08 15:08 PT, log `/tmp/bronze_fetch.log`.

- **55.3%** of 4,137,834 accessions at 23:17 PT, rate steady ~5.2/s.
- **~4.1 days remain** → finishes approximately **2026-09-15**.
- Check it with: `ssh derekg@100.78.9.66 'tail -2 /tmp/bronze_fetch.log'`
- `ops_bronze_topup` (Dagster, hourly at :20) runs alongside it safely — the
  fetcher takes an advisory lock.

**When it reaches 100%, and not before:** run `scripts/verify_bronze.py` on
Studio. It has five independent properties, was written to fail, and **has
never been run**. That is the single most valuable queued task in this repo.

---

## Next steps, in order

### 1. Extend the watchdog to check status, not just recency  ← START HERE

**This is a gap I introduced today and did not close.** `SERVICE_HEARTBEAT` in
`scripts/offbox_watchdog.py` asks when a service last *ran*. It does not ask
whether the run *succeeded*.

That blind spot is not hypothetical: `notification_scanner` wrote a
`pipeline_runs` row every five minutes for seventeen days while **every one of
4,182 runs failed**. The check added today would have reported it green
throughout.

The fix: `pipeline_runs` already has a `status` column (`'ok'` / `'failed'`).
Extend the `SERVICE_HEARTBEAT` query to select the latest status per service
and fail when the most recent run is not `ok`, keeping the existing staleness
check alongside it — a service can be both stale and failing, and those are
different problems with different causes.

Verify by running `/usr/bin/python3 scripts/offbox_watchdog.py --dry-run` from
the Mini. `--dry-run` never sends an alert and is safe to run repeatedly.

### 2. Leave Checkpoint 1 alone until ~2026-10-01

Phase 1 shipped today. The CTA now renders for every non-Pro visitor instead of
16% of them. **Do not read the result yet** — at ~700 landers/month the sample
needs two to three weeks.

When reading it, use the three metrics in the plan, not the old "CTA-seen
rate": mount rate → >90%, then `follow_cta_viewed ÷ follow_cta_shown`, then
`follow_cta_clicked ÷ follow_cta_viewed`.

**`follow_cta_shown` fires on MOUNT and always has.** It never meant anyone saw
anything. Reading it as "saw the CTA" produced a confident wrong diagnosis that
survived a week. `follow_cta_viewed` is the viewport event. Do not merge them.

### 3. Small, safe, self-contained

- **`offbox_watchdog`'s own trigger** is still `StartInterval=1800`. It runs on
  the Mini, whose uptime is short, so it has not been bitten — but it is the
  last one. Fix with
  `python3 scripts/launchd_interval_to_calendar.py <plist>`, then
  `launchctl bootout gui/$(id -u)/com.derekg.offbox-watchdog` and `bootstrap`.
- **Default `tier` metadata on signup.** The five newest Clerk users carry no
  `tier` at all. Backlog item 2.4.

### 4. Do NOT start without Derek

- **Phase 2 (gating changes).** Needs his explicit answer on free users seeing
  all history. Question 3 in the plan, still unanswered.
- **AdSense.** Blocked on Checkpoint 3 (50k pageviews/month). Currently ~600.
- **Anything that restarts Dagster, unloads a plist, or stops a service.**
  Present the data and recommend; do not act.

---

## Traps that cost real time today

**Check `pipeline_runs.status`, not just `max(started_at)`.** A service writing
rows on schedule can be failing every single one. See
`project_2026-09-10_notification_scanner_null_tier` in memory.

**`launchctl list` reports `exit 0` for a job that is not firing.** Six agents
were dead 57 hours while every launchctl check said fine. Check log mtime or
`pipeline_runs`.

**Never write a new `.plist`.** Scheduling goes in Dagster. The three watchdogs
that stayed on launchd are a deliberate exemption with a written argument in
`dataplane/deploy/scheduled_work.yaml`; they are not precedent.

**Never build a shell command with a live API key in a variable.** Doing that
today printed the live Stripe secret into a transcript and forced a rotation.
Use Python + `urllib`. Keys are in `/Users/derekg/trading-framework/.env`
(`POSTHOG_PERSONAL_API_KEY`, `POSTHOG_PROJECT_ID=377137`, `CLERK_SECRET_KEY`,
`STRIPE_SECRET_KEY`). PostHog's API host is `us.posthog.com`, **not** the
`us.i.posthog.com` ingestion host in the env var, and personal keys are
project-scoped so `/api/projects/` 403s — use `/api/projects/@current/`.

**Before restarting any long-dead sender, size its backlog.** Check the
watermark and the tier in `api/notification_policy.py`. Today's scanner had 618
groups × 5 users queued; it was safe only because the affected types are
`DIGEST` and `FEED_ONLY`.

**Stocktwits: velocity, not content, suspended the account twice.** 45 minutes
between posts, minimum. The generator emits a schedule for exactly this reason.

---

## Open questions for Derek — all still unanswered

1. Is form4.app verified in **Search Console**, and what does Coverage say? The
   sitemap now publishes 51,747 insider URLs across three chunks where it
   published 45,000 in one rejected file; nothing confirms Google is ingesting
   it.
2. Was the **2026-08-30 chargeback** disputed with Stripe?
3. **Phase 2 gating**: is it acceptable for free users to see all history?

---

## 2026-09-16 — the sites went down again for 5h11m; full RCA written

Same class as 09-15, and the 09-15 explanation was wrong twice over: inotify
was not the cause (the master died again with it off), and Lima does not
re-add the forwards when a new master appears (my own `ssh -L` stopgap
clients did that through the mux). The real defect is architectural: the
edge of all three sites is a set of `ssh -L` forwards inside ONE unsupervised
ssh ControlMaster that Lima detaches at boot. When it closes — cleanly, cause
still unidentified — nothing on the box recreates anything until a human
runs ssh. Read `docs/rca_2026-09-16_colima_ssh_master.md`.

**Built, not yet installed (needs Derek's yes — new launchd agent on prod):**
`scripts/lima_master_keepalive.sh` + `scripts/launchd/com.derekg.lima-master-keepalive.plist`.
Every 15 s it forwards whatever is missing on the live master, or becomes
the master itself and then forwards docker.sock + every `docker ps`
published port. Recovery ≤ ~20 s, no container touched. Its reconcile pass
was run once by hand against the live master and restored the 443 forward
the stopgap had not carried. `tests/unit/test_lima_keepalive_ports.py`
covers the port parse.

**Installed and proven 2026-09-16 11:03 (Derek: "run whatever you need to").**
`launchctl bootstrap` loaded the agent as "not running" and RunAtLoad did
nothing — the same launchd behaviour as the tunnels and Colima on this box —
so `launchctl kickstart -k gui/$(id -u)/com.derekg.lima-master-keepalive`
was needed. Controlled failover: master killed 11:03:25 → keepalive took over
11:03:31 → new master, 80/443/8080/8082 and docker.sock all back 11:03:32 →
form4.app 200 at +9 s. The hand-run docker.sock stopgap was retired. The
supervised master is a child of the keepalive (`ps -o ppid`), so a death is
now a ~7 s blip logged to `logs/lima-master-keepalive.log`.

**Also fixed:** `scripts/uptime_monitor.sh` exits 1 on a failed check, so
`pipeline_runs.form4_uptime` stops saying `ok` through an outage. (A deploy
now produces exactly one `failed` run — the frontend restart — then `ok`.)

**Second finding, 11:10–11:20:** last night's `brew services restart colima`
at 23:25:37 stopped six unrelated long-running agents and launchd never
restarted them: dagster-daemon (every Dagster schedule silent 23:25 → 11:17),
the GitHub deploy runner (today's push queued until kickstarted at 11:14),
dagster-webserver, Ollama, and the two tunnels fixed that night. All
kickstarted and verified: Dagster scheduling resumed 11:17:15, deploy
succeeded, UI 200, Ollama 200. The watchdog now names a loaded-but-not-running
agent with its kickstart command (`MUST_RUN_AGENTS` in
`scripts/offbox_watchdog.py`; live dry run clean). **Rule: a Colima restart
is a full-service restart — kickstart and verify every agent in that list.**

**Still open:** what sends the master its clean disconnect (see the RCA's
"Open question" — the keepalive log now gives the exact second to correlate
against `log show`); tunnels to the VM IP as the structural fix; the two
tunnels that ignore `KeepAlive`.


---

## 2026-09-16 (afternoon) — the funnel, and why nobody clicked follow

Derek's read, confirmed in code: the anonymous follow band said "7-day free
trial, no credit card required" — the Pro trial's line under a free feature.
Shipped: free-account copy on the band, `FollowInline` under the header of
insider and company pages (placement-tagged events), `follow_cta_shown` now
waits for PostHog init (was dropped for 95% of landers), and the sign-up page
finally sends people back with `?follow=` and says what the account is for.
Full numbers in memory `project_2026-09-10_growth_baseline.md` (09-16 section).
Read again ~09-30. Copy rule: plain sentences, nothing that reads as AI.

Still open from the funnel read: new accounts get no follows, no `tier`
metadata, and their onboarding answers are not saved (`user_profiles` has 2
rows; `onboarding_complete` fired 4×) — activation defaults are the next
item. Pro's value proposition (immediacy) is unstated on /pricing.


## 2026-09-17 — accounts are free; the trial is a choice

The trial was the account model: tier derived from `createdAt` on both
sides, nav countdown, site-wide banner, 16 "Start Free Trial" surfaces, a
six-hourly trial email funnel (41 sent). All replaced: tier = Clerk metadata
or free; Pro trial only at checkout (7 days, card on file, once per
customer); "Sign up free" for visitors; four-email free-account sequence in
`pipelines/trial_emails.py` (name kept — Dagster asset and table key on it).
Onboarding saves again (`default_strategy` column, migration applied).
Still open: 2.2d (alerts for the strategy chosen in onboarding, a welcome
that shows a real alert) and 2.3 (what Pro is for, on /pricing).


## 2026-09-17 (late) — the rest of the free-account work, and a read date

Shipped in one push: onboarding seeds follows from the chosen book and the
welcome shows a real alert (2.2d); the 90-day history wall is down —
`HISTORY_IS_FREE`, record free for everyone, analysis (grades, clusters,
book trade log) still Pro (2.1/2.3); StockTwits generator no longer posts a
filer as his own co-buyer (name-order variants are one person) or a market
maker on both sides of an ETF (two-sided flow within 5 days is dropped);
`scripts/funnel_read.py` for the checkpoint read.

**Read the funnel on ~2026-09-30:** `python3 scripts/funnel_read.py`. What
matters: viewed→clicked by placement, `follow_completed`, landers→signups,
and whether the day-3 email is going to accounts that follow someone.

**DONE 2026-09-18 — the orphan repair is applied** (both windows; 47,807
rows filled, 15,237 `sec_form345` copies marked duplicate, 245 unmatched
remain; backup `repair_orphans_20260914_before`). The dedupe had to be
rewritten as a join — the EXISTS form hit the statement timeout. Commands
kept for the record:
```
cd /Users/derekg/trading-framework && PYTHONPATH=$PWD /opt/homebrew/bin/python3 scripts/repair_orphan_trades_from_silver.py --window 2026 --apply
PYTHONPATH=$PWD /opt/homebrew/bin/python3 scripts/repair_orphan_trades_from_silver.py --window 2016-2019 --apply
```
Dry runs: 13,600 of 13,780 (2026) and 34,207 of 34,272 (2016–19) fill;
backup table `repair_orphans_20260914_before`; dedupe marks same-insider
`sec_form345` copies.


## 2026-09-18 (evening) — Gold, the pipeline on a schedule, and one decision

Gold v0 is built and the Bronze→Silver→Gold pipeline runs itself now
(index refresh, top-up, keep_up, gold refresh — all Dagster, all in the
registry). Silver has the 10b5-1 flag by trades' own rule, over the whole
corpus. `docs/gold_cutover_plan.md` has round 2 of the parity.

**Decision needed:** `trades` under-flags 10b5-1 — 300,018 rows / 122,687
filings that Silver says were planned are shown as decisions, including
9,731 purchases in the grading population the 08-24 A-List rule excluded.
Interim repair = one UPDATE from Silver's flag (trigger reclassifies) plus
a grade recompute the same night. Reversible. Recommended yes.


## 2026-09-20 — Google traffic to zero; the edge was dropping connections

Google visitors 13–16/day → 0 from 09-18. Found: the keepalive's master ran
under launchd's default 256-fd limit and dropped 302 connections (09-16, 19,
20) under crawler bursts, on top of the 5h11m 502 outage of 09-16. Fixed
09-20 23:58 (fd limit 65536, docker-socket recovery, fd logging, crawl-delay
for Ahrefs/Semrush). Site is clean for Googlebot now (200s, canonicals, no
noindex, sitemaps serve, 65 crawls in 7 h). **Derek: open Search Console →
Crawl stats (response codes by day) and Pages (server-error exclusions),
then resubmit the sitemap.** That is the only view of Google's side; a
service account on the property would let the funnel script read it.
RCA addendum in docs/rca_2026-09-16_colima_ssh_master.md.

Still pending Derek's yes: the 10b5-1 interim repair to `trades`
(docs/gold_cutover_plan.md, round 2).


## 2026-09-21 overnight — what was done, what needs a yes

Picked the Gold cutover thread off the backlog (nothing needed a decision):
- **Step 4 done:** `gold.trusted_value`; the finding underneath it is that
  `prices.daily_prices` is split-adjusted and filed prices are not, so
  `outside_band` is mostly splits (memory: feedback_daily_prices_are_split_adjusted).
- **Step 6 planned:** `scripts/merge_insider_identities.py` dry run →
  `reports/identity_merge_plan.md`: 2,515 groups mergeable, 1,659 review,
  and a separate defect — 5,624 joint filings with one owner's CIK on every
  line. **`--apply` needs a yes.**
- **Step 7 written:** `docs/gold_cutover_design.md`. **Read this one.**
- Watchdog now fetches an insider page, a company page, robots.txt and the
  sitemap index as Googlebot every tick and checks canonical/noindex/size.
- Sitemap: the API caches the 5 MB URL list for an hour; the index no longer
  pulls it; the frontend image lets Next write its ISR cache (EACCES gone).
- Edge: the keepalive's fd limit fix from 23:58 — zero accept errors since.

**Three decisions waiting on Derek:** the 10b5-1 interim repair to `trades`;
the identity merge `--apply`; the cutover design. Plus Search Console for
the Google drop.


## 2026-09-21 evening — the books rebuilt on clean grades; the alert pipeline fixed

Derek asked which book to put personal money behind, then to publish the
actual trades correctly and fix the pipeline. Full account in
docs/published_returns_methodology.md § "2026-09-21" and CLAUDE.md.

- **Shipped `b805476`**: runner sizes off its own ledger (it was summing the
  simulated book — A-List had NEVER alerted; Breakout's alerts rode a 07:00
  race); relative guardrail + yaml caps reset; live stop on the close after
  16:05 ET; dedup by filing; simulator replaces books atomically;
  `build_pit_scores` now applies the three grade rules; `--clear` keeps
  career columns; `backfill_pit_grades --rebuild`. Six tests. The two prior
  deploys had failed silently on uncommitted Studio edits (identical to the
  committed files) — restored.
- **Rebuilt history** (restore points `*_pre_strict_20260921`): career grades
  5.3 h, V2 scores, pit grades, signals, books. **A-List is now −0.9%
  blended against SPY 13.4% (overlay −1.6%), 77% daily drawdown, beat SPY in
  0 of its 3-year windows. Breakout 19.4% (API) / 17.1% (overlay) vs 13.8%.
  Dip Buys unchanged.** The site serves these now.
- Sanity: the six late-filed positions moved exactly as the strict re-score
  predicted; the PIT step scored exactly the 157,667 discretionary-buy
  filings it should; alert rows untouched.

**Decisions waiting on Derek:**
1. **A-List Buys** is a measured losing book on the public performance page.
   Retire it (`ACTIVE_STRATEGIES` / `STRATEGY_CONFIG`, the yaml stays), or
   re-research it with a second independent filter. The 06:00 PT runner will
   now actually emit its alerts, on this book, tomorrow.
2. **Which blended figure to publish** — the API's (under-allocates as the
   book grows, currently the homepage) or the overlay's (sizes as the rules
   say). They differ by 2.3 points on Breakout.
3. **Dip Buys** — 14 trades, last entry 2025-04-14, behind SPY: retire.
4. The insider pages' **Recent Form** grade now shows on far fewer insiders
   (scored V2 rows 976,821 → 102,703, by design). Say so on the page or drop
   the column.
5. Sizing note for the original question: on clean grades Breakout is the
   only candidate, and its edge is thinner and more concentrated than this
   morning's read (top 10 trades = 167% of P&L). The `strategy_health` job
   may page on A-List's drawdown at 17:00 tomorrow.

Still pending from before: the 10b5-1 interim repair, the identity merge
`--apply`, the cutover design, Search Console.
